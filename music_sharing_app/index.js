/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import RouterPage from "./src/private/pageBar/RouterPage";

AppRegistry.registerComponent(appName, () => RouterPage);
